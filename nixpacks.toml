# Garante build do Vite antes do start (variáveis VITE_* vêm do painel Railway → Variables)

[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["npm ci"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "npm start"
